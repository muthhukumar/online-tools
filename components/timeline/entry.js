const Entry = ({ entry }) => {
    return (
        <div className="max-w-full ml-3 py-3 break-words max-w-screen-md border-b border-gray-200">
            {entry}
        </div>
    );
};

export default Entry;
